<h3>Buat dari Awal</h3>

```
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/itozt/Pemrograman-Perangkat-Bergerak.git
git push -u origin main
```

<h3>Folder Baru Ditambahkan</h3>

```
git add "Pertemuan 7/"
git commit -m "comment"
git push -u origin main
```
